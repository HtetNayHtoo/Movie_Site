<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid ms-5">
    <a class="navbar-brand col-lg-5" href="#">MOVIE</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse col-lg-7" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item ms-3">
          <%= link_to 'Home', movies_path, class: 'nav-link active' %>
        </li>
        <li class="nav-item dropdown ms-3">
          <a class="nav-link dropdown-toggle" href="#" id="navMenu" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Category
          </a>
          <ul class="dropdown-menu" aria-labelledby="navMenu">
           <% @movies.all.collect(&:category).uniq.each do |category|%>
              <li class="dropdown-item">
                <%= link_to category, movies_path(cate: category) %>
              </li>
            <% end %>
            <!--<% @movies.each do |movie|%>
              <li class="dropdown-item">
                <%= link_to movie.category, movies_path(cate: movie.category) %>
              </li>
            <% end %>-->
          </ul>
        </li>
        <li class="nav-item ms-3">
          <%= link_to 'Order', orders_path, class: 'nav-link' %>
        </li>
        <li class="nav-item ms-3">
         <%= link_to 'Profile', current_user, class: 'nav-link' %>
        </li>
       <% if current_user.user_type == "Admin" %>
          <li class="nav-item ms-3">
           <%= link_to 'User List', users_path, class: "btn btn-sm btn-info text-white nav-link" %>
        </li>
       <% end %>
       <li>
        <%= form_tag(movies_path, :method => "get") do %>
          <!--<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Search</button>-->
          <%= text_field_tag :search, params[:search], :placeholder => "Search"%>
          <%= submit_tag "Search", class:"btn btn-primary btn-sm float-end"%>
        <% end %>
        </li>
        <li class="nav-item ms-3">
           <%= link_to 'Log Out', logout_path, class: "btn btn-sm btn-danger text-white nav-link" %>
        </li>
      </ul>
    </div>
  </div>
</nav>